import React from 'react';

export default function ResidentRootLayout({ children }: { children: React.ReactNode }) {
    // Route groups under /resident define their own chrome.
    return children;
}

